[alias]
	cl = clone
	st = status
	ad = add
	cm = commit
	co = checkout
	br = branch
	lg = log
	gr = lg --graph --oneline --all
	lgn = lg --name-only --oneline
	adi = ad -i
	diffs = diff --staged
	up = fetch --all --prune
